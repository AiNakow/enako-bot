# Project Working Agreements

## Implementation principles

- 保持实现精简，避免重复代码、重复抽象和功能重叠的执行路径。
- 每项功能应有一个明确、统一的主要执行路径。
- 不要为未经确认的旧版本、旧调用方或假设场景添加兼容层、回退路径、别名、双写或静默降级。
- 只有在项目已有明确兼容要求，或用户明确提出时，才保留兼容行为。
- 替换旧实现时，同步更新所有调用方、测试和文档，并删除已经失效的旧路径。
- 优先扩展现有抽象，不要创建功能相近的平行实现。
- 如果兼容需求或架构意图不明确，先说明具体影响，再请求确认。
- 修改应聚焦当前任务，避免无关重构和顺手增加功能。

## Test principles

- 在实现每一阶段的功能方案后，测试应该尽可能的完整的覆盖当前所有功能链路，并报告当前的测试覆盖率。
- 测试方案还应包含真实场景链路测试，保证整个链路完整闭环。

## Communication

- 回复保持简洁，优先说明结果、风险和需要用户决定的事项。
- 不重复解释显而易见的代码或已确认的信息。